import entradaStyles from '../styles/Entrada.module.css';

const Entrada = () => {
    return (
        <div className={entradaStyles.container} id={'cortina-entrada'}>
            <img src={'assets/logo_texto.svg'}/>
        </div>
    )
};

export default Entrada;
